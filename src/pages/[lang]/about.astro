---
import BaseLayout from '@/layouts/BaseLayout.astro';
import PageHeader from '@/components/PageHeader.astro';
import { normalizeLang } from '@/lib/i18n';
import { getPublicCopy } from '@/lib/publicCopy';
const lang = normalizeLang(Astro.params.lang);
const copy = getPublicCopy(lang);
---
<BaseLayout title={copy.about.title} lang={lang} description={copy.about.description}>
  <PageHeader eyebrow={copy.portalEyebrow} title={copy.about.title} description={copy.about.description} />
  <div class="terminal-panel prose-stellar p-6">
    <p>{copy.about.body}</p>
  </div>
</BaseLayout>
